spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/eslamhesham-dev/ecommerce-config-repo.git
          default-lablel: main
          clone-on-start: true

server:
  port: 8888

management:
  endpoints:
    web:
      exposure:
        include: health, info
